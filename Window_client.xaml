<Window x:Class="Chat_bot_multicast.Window_client"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Chat_bot_multicast"
        mc:Ignorable="d"
        Title="Window_client" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="179*"></RowDefinition>
            <RowDefinition Height="38*"></RowDefinition>
            <RowDefinition Height="38*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <TextBox x:Name="tb_chat" Grid.Row="0" Text="{Binding MyString}"/>
        <TextBox x:Name="tb_send" Grid.Row="1" Text="Enter message here..." FontSize="16" GotFocus="tb_send_GotFocus" LostFocus="tb_send_LostFocus"/>
        <Button Content="Send" Grid.Row="2" FontSize="24" Click="Button_Click"/>

    </Grid>
</Window>
